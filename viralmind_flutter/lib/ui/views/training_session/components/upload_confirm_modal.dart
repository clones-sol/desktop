import 'package:flutter/material.dart';

class UploadConfirmModal extends StatelessWidget {
  const UploadConfirmModal({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
